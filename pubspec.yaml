name: worklogix_flutter
description: A simple app for tracking employee hours.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^1.10.25

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true